# The active exercise: the one every build, stage, test and style recipe operates on.
# Change it here to switch exercises; `just EXERCISE=<name> <recipe>` overrides it for one run.
EXERCISE := "tiny_compute"
