# AGENTS – linux-kernel-driver-sandbox

- Everything under `exercises/*/driver/` and `exercises/*/userspace/` is the
  human's exercise work.
  Change a file's content there only when the human's own message names that
  file and asks for the change; the human describing their own next step is
  not that ask. A dispatch brief counts only by quoting that message. Moving
  or renaming these paths within a framework task, and building or running
  them, are not content changes. `just format` rewrites these files in place
  and is the human's to run; an agent runs it only when asked to.
