# THESIS – linux-kernel-driver-sandbox

A sandbox for learning Linux kernel driver development by doing it. The human writes the kernel code. Agent assistance is limited to project setup and review. Otherwise, what's the point?
